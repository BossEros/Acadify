@model ASI.Basecode.Data.Models.Class

@{
    Layout = null;
}

<div class="space-y-6">
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-y-4 gap-x-6">
        <div>
            <p class="text-sm text-gray-500">EDP Code</p>
            <p class="font-medium text-gray-800">@Model.Id</p>
        </div>

        <div>
            <p class="text-sm text-gray-500">Course Code</p>
            <p class="font-medium text-gray-800">@Model.Course?.CourseCode</p>
        </div>

        <div>
            <p class="text-sm text-gray-500">Course Name</p>
            <p class="font-medium text-gray-800">@Model.Course?.CourseName</p>
        </div>

        <div>
            <p class="text-sm text-gray-500">Teacher</p>
            <p class="font-medium text-gray-800">@($"{Model.Teacher?.FirstName} {Model.Teacher?.LastName}")</p>
        </div>

        <div>
            <p class="text-sm text-gray-500">Semester</p>
            <p class="font-medium text-gray-800">@Model.Semester</p>
        </div>

        <div>
            <p class="text-sm text-gray-500">Year</p>
            <p class="font-medium text-gray-800">@Model.YearLevel</p>
        </div>

        <div>
            <p class="text-sm text-gray-500">Schedule</p>
            <p class="font-medium text-gray-800">@Model.Schedule</p>
        </div>

        <div>
            <p class="text-sm text-gray-500">Join Code</p>
            <p class="font-medium text-gray-800">@Model.JoinCode</p>
        </div>

        <div>
            <p class="text-sm text-gray-500">Status</p>
            @if (Model.IsActive)
            {
                <span class="inline-block px-3 py-1 rounded-full text-sm font-medium bg-green-100 text-green-700">
                    Active
                </span>
            }
            else
            {
                <span class="inline-block px-3 py-1 rounded-full text-sm font-medium bg-gray-200 text-gray-700">
                    Inactive
                </span>
            }
        </div>
    </div>

    <div class="flex justify-end pt-4 border-t">
        <button type="button" 
                class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700"
                onclick="closeModal('modalDetails')">
            Close
        </button>
    </div>
</div>
