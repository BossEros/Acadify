@model ASI.Basecode.WebApp.ViewModels.AccountManagement.UserListViewModel
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "Account Management";
}

<div class="flex flex-col space-y-4">
    @if (!string.IsNullOrEmpty(Model.Message))
    {
        <div class="p-4 rounded @(Model.MessageType == "success" ? "bg-green-100 text-green-700" : "bg-red-100 text-red-700")">
            @Model.Message
        </div>
    }

    <div class="flex items-start justify-between">
        <div>
            <h1 class="text-2xl font-semibold">Account Management</h1>
            <p class="text-gray-500">Manage user accounts</p>
        </div>
        <div class="w-72">
            <label class="sr-only" for="searchUserId">Search by User ID</label>
            <div class="relative">
                <input id="searchUserId" type="text" placeholder="Search by User ID" class="w-full rounded border border-gray-300 pl-3 pr-10 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500" />
                <button type="button" id="btnSearchGlobal" class="absolute right-1 top-1/2 -translate-y-1/2 text-gray-600 hover:text-gray-800 p-1" aria-label="Search">
                    <span class="material-icons"></span> 
                </button>
            </div>
        </div>
    </div>

    <div class="flex items-center space-x-3">
        <button type="button" id="openEnroll" class="bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded">Enroll Student</button>
        <button type="button" id="openAssign" class="bg-purple-600 hover:bg-purple-700 text-white px-4 py-2 rounded">Assign Teacher</button>
        <button type="button" id="openEdit" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded">Edit User</button>
        <button type="button" id="openDelete" class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded">Delete User</button>
    </div>

    <div class="bg-white rounded-lg shadow p-4">
        <table class="w-full border-collapse">
            <thead class="bg-gray-50">
                <tr>
                    <th class="p-3 text-left text-xs font-semibold text-gray-600">USER ID</th>
                    <th class="p-3 text-left text-xs font-semibold text-gray-600">FIRST NAME</th>
                    <th class="p-3 text-left text-xs font-semibold text-gray-600">LAST NAME</th>
                    <th class="p-3 text-left text-xs font-semibold text-gray-600">EMAIL ADDRESS</th>
                    <th class="p-3 text-left text-xs font-semibold text-gray-600">ROLE</th>
                    <th class="p-3 text-left text-xs font-semibold text-gray-600">STATUS</th>
                </tr>
            </thead>
            <tbody id="usersTbody">
                @foreach (var user in Model.Users)
                {
                    <tr class="border-b" data-user-id="@user.Id.ToString("D6")">
                        <td class="p-3">@user.Id.ToString("D6")</td>
                        <td class="p-3">@user.FirstName</td>
                        <td class="p-3">@user.LastName</td>
                        <td class="p-3">@user.Email</td>
                        <td class="p-3">@user.Role</td>
                        <td class="p-3">
                            @if (user.IsActive)
                            {
                                <span class="inline-block rounded-full bg-green-100 text-green-700 text-xs px-2 py-1">Active</span>
                            }
                            else
                            {
                                <span class="inline-block rounded-full bg-red-100 text-red-700 text-xs px-2 py-1">Inactive</span>
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    @Html.AntiForgeryToken()

    <!-- Enroll Student Modal -->
    <div id="modalEnroll" class="hidden fixed inset-0 z-50 items-center justify-center bg-gray-900/50 backdrop-blur">
        <div class="bg-white w-full max-w-lg rounded-xl shadow p-6">
            <h3 class="text-xl font-semibold mb-4">Enroll Student</h3>
            <form id="formEnroll" class="space-y-4">
                <div>
                    <label class="block text-sm text-gray-700 mb-1">User ID</label>
                    <input name="UserId" type="number" class="w-full rounded border border-gray-300 px-3 py-2" placeholder="Enter User ID" required />
                </div>
                <div>
                    <label class="block text-sm text-gray-700 mb-1">EDP Code</label>
                    <input name="EdpCode" type="text" class="w-full rounded border border-gray-300 px-3 py-2" placeholder="Enter EDP Code" required />
                </div>
                <div class="flex justify-end space-x-2 pt-2">
                    <button type="button" data-close="modalEnroll" class="px-4 py-2 rounded border">Cancel</button>
                    <button type="submit" class="px-4 py-2 rounded bg-green-600 text-white">Enroll Student</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Assign Teacher Modal -->
    <div id="modalAssign" class="hidden fixed inset-0 z-50 items-center justify-center bg-gray-900/50 backdrop-blur">
        <div class="bg-white w-full max-w-lg rounded-xl shadow p-6">
            <h3 class="text-xl font-semibold mb-4">Assign Teacher</h3>
            <form id="formAssign" class="space-y-4">
                <div>
                    <label class="block text-sm text-gray-700 mb-1">User ID</label>
                    <input name="UserId" type="number" class="w-full rounded border border-gray-300 px-3 py-2" placeholder="Enter User ID" required />
                </div>
                <div>
                    <label class="block text-sm text-gray-700 mb-1">EDP Code</label>
                    <input name="EdpCode" type="text" class="w-full rounded border border-gray-300 px-3 py-2" placeholder="Enter EDP Code" required />
                </div>
                <div class="flex justify-end space-x-2 pt-2">
                    <button type="button" data-close="modalAssign" class="px-4 py-2 rounded border">Cancel</button>
                    <button type="submit" class="px-4 py-2 rounded bg-purple-600 text-white">Assign Teacher</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Edit User Modal -->
    <div id="modalEdit" class="hidden fixed inset-0 z-50 items-center justify-center bg-gray-900/50 backdrop-blur">
        <div class="bg-white w-full max-w-2xl rounded-xl shadow p-6">
            <h3 class="text-xl font-semibold mb-4">Edit User</h3>
            <div class="space-y-4">
                <div class="flex space-x-2">
                    <input id="editSearchUserId" type="number" placeholder="Enter User ID to search" class="flex-1 rounded border border-gray-300 px-3 py-2" />
                    <button type="button" id="btnEditFind" class="px-3 py-2 rounded bg-gray-800 text-white">Search</button>
                </div>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                        <label class="block text-sm text-gray-700 mb-1">First Name</label>
                        <input id="editFirstName" class="w-full rounded border border-gray-200 bg-gray-100 px-3 py-2" disabled />
                    </div>
                    <div>
                        <label class="block text-sm text-gray-700 mb-1">Last Name</label>
                        <input id="editLastName" class="w-full rounded border border-gray-200 bg-gray-100 px-3 py-2" disabled />
                    </div>
                    <div>
                        <label class="block text-sm text-gray-700 mb-1">Role</label>
                        <input id="editRole" class="w-full rounded border border-gray-200 bg-gray-100 px-3 py-2" disabled />
                    </div>
                </div>
                <form id="formEdit" class="space-y-4">
                    <input type="hidden" name="UserId" id="editUserIdHidden" />
                    <div>
                        <label class="block text-sm text-gray-700 mb-1">Email Address</label>
                        <input name="Email" id="editEmail" type="email" class="w-full rounded border border-gray-300 px-3 py-2" placeholder="Enter email address" required />
                    </div>
                    <div>
                        <label class="block text-sm text-gray-700 mb-1">Status</label>
                        <select name="IsActive" id="editStatus" class="w-full rounded border border-gray-300 px-3 py-2" required>
                            <option value="">Select Status</option>
                            <option value="true">Active</option>
                            <option value="false">Inactive</option>
                        </select>
                    </div>
                    <div class="flex justify-end space-x-2 pt-2">
                        <button type="button" data-close="modalEdit" class="px-4 py-2 rounded border">Cancel</button>
                        <button type="submit" class="px-4 py-2 rounded bg-blue-600 text-white">Save</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <!-- Delete User Modal -->
    <div id="modalDelete" class="hidden fixed inset-0 z-50 items-center justify-center bg-gray-900/50 backdrop-blur">
        <div class="bg-white w-full max-w-2xl rounded-xl shadow p-6">
            <h3 class="text-xl font-semibold mb-4">Delete User</h3>
            <div class="space-y-4">
                <div class="flex space-x-2">
                    <input id="deleteSearchUserId" type="number" placeholder="Enter User ID to search" class="flex-1 rounded border border-gray-300 px-3 py-2" />
                    <button type="button" id="btnDeleteFind" class="px-3 py-2 rounded bg-gray-800 text-white">Search</button>
                </div>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <div>
                        <label class="block text-sm text-gray-700 mb-1">First Name</label>
                        <input id="deleteFirstName" class="w-full rounded border border-gray-200 bg-gray-100 px-3 py-2" disabled />
                    </div>
                    <div>
                        <label class="block text-sm text-gray-700 mb-1">Last Name</label>
                        <input id="deleteLastName" class="w-full rounded border border-gray-200 bg-gray-100 px-3 py-2" disabled />
                    </div>
                    <div>
                        <label class="block text-sm text-gray-700 mb-1">Email Address</label>
                        <input id="deleteEmail" class="w-full rounded border border-gray-200 bg-gray-100 px-3 py-2" disabled />
                    </div>
                    <div>
                        <label class="block text-sm text-gray-700 mb-1">Role</label>
                        <input id="deleteRole" class="w-full rounded border border-gray-200 bg-gray-100 px-3 py-2" disabled />
                    </div>
                </div>
                <form id="formDelete" class="space-y-4">
                    <input type="hidden" name="UserId" id="deleteUserIdHidden" />
                    <div class="flex justify-end space-x-2 pt-2">
                        <button type="button" data-close="modalDelete" class="px-4 py-2 rounded border">Cancel</button>
                        <button type="submit" class="px-4 py-2 rounded bg-red-600 text-white">Delete</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<script>
    (function () {
        const qs = (s) => document.querySelector(s);
        const token = document.querySelector('input[name="__RequestVerificationToken"]').value;

        function open(id) { const el = qs('#' + id); el.classList.remove('hidden'); el.classList.add('flex'); }
        function close(id) { const el = qs('#' + id); el.classList.add('hidden'); el.classList.remove('flex'); }

        qs('#openEnroll').addEventListener('click', () => open('modalEnroll'));
        qs('#openAssign').addEventListener('click', () => open('modalAssign'));
        qs('#openEdit').addEventListener('click', () => open('modalEdit'));
        qs('#openDelete').addEventListener('click', () => open('modalDelete'));

        document.querySelectorAll('[data-close]').forEach(b => b.addEventListener('click', (e) => close(e.target.getAttribute('data-close'))));

        async function postForm(url, data) {
            const res = await fetch(url, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json', 'RequestVerificationToken': token },
                body: JSON.stringify(data)
            });
            return await res.json();
        }

        async function findUser(userId) {
            const res = await fetch(`@Url.Action("FindUser")?userId=${encodeURIComponent(userId)}`);
            return await res.json();
        }

        // Enroll
        qs('#formEnroll').addEventListener('submit', async (e) => {
            e.preventDefault();
            const data = Object.fromEntries(new FormData(e.target).entries());
            const result = await postForm('@Url.Action("EnrollStudent")', data);
            alert(result.message);
            if (result.success) { location.reload(); }
        });

        // Assign
        qs('#formAssign').addEventListener('submit', async (e) => {
            e.preventDefault();
            const data = Object.fromEntries(new FormData(e.target).entries());
            const result = await postForm('@Url.Action("AssignTeacher")', data);
            alert(result.message);
            if (result.success) { location.reload(); }
        });

        // Edit - search
        qs('#btnEditFind').addEventListener('click', async () => {
            const userId = qs('#editSearchUserId').value;
            if (!userId) return;
            const u = await findUser(userId);
            if (!u || !u.success) { alert(u?.message || 'User not found'); return; }
            qs('#editFirstName').value = u.data.firstName;
            qs('#editLastName').value = u.data.lastName;
            qs('#editEmail').value = u.data.email;
            qs('#editRole').value = u.data.role;
            qs('#editUserIdHidden').value = u.data.id;
            qs('#editStatus').value = u.data.isActive.toString().toLowerCase();
        });

        // Edit - submit
        qs('#formEdit').addEventListener('submit', async (e) => {
            e.preventDefault();
            const userId = qs('#editUserIdHidden').value;
            if (!userId) {
                alert('Please search for a user first.');
                return;
            }
            const formData = Object.fromEntries(new FormData(e.target).entries());
            const data = {
                UserId: parseInt(formData.UserId),
                Email: formData.Email,
                IsActive: formData.IsActive === 'true'
            };
            const result = await postForm('@Url.Action("EditUser")', data);
            alert(result.message);
            if (result.success) { location.reload(); }
        });

        // Delete - search
        qs('#btnDeleteFind').addEventListener('click', async () => {
            const userId = qs('#deleteSearchUserId').value;
            if (!userId) return;
            const u = await findUser(userId);
            if (!u || !u.success) { alert(u?.message || 'User not found'); return; }
            qs('#deleteFirstName').value = u.data.firstName;
            qs('#deleteLastName').value = u.data.lastName;
            qs('#deleteEmail').value = u.data.email;
            qs('#deleteRole').value = u.data.role;
            qs('#deleteUserIdHidden').value = u.data.id;
        });

        // Delete - submit
        qs('#formDelete').addEventListener('submit', async (e) => {
            e.preventDefault();
            const data = Object.fromEntries(new FormData(e.target).entries());
            const result = await postForm('@Url.Action("DeleteUser")', data);
            alert(result.message);
            if (result.success) { location.reload(); }
        });

        // Global Search by User ID - reorder and highlight
        const tbody = qs('#usersTbody');
        const originalRows = Array.from(tbody.children);
        function resetTable() {
            tbody.innerHTML = '';
            originalRows.forEach(r => { r.classList.remove('ring-2','ring-blue-300'); tbody.appendChild(r); });
        }
        function reorderForSearch(term) {
            if (!term) { resetTable(); return; }
            const rows = Array.from(tbody.children);
            const match = rows.find(r => r.dataset.userId && r.dataset.userId.startsWith(term.padStart(6,'0')));
            if (!match) return; // keep as-is if no match
            tbody.prepend(match);
            match.classList.add('ring-2','ring-blue-300');
            setTimeout(() => match.classList.remove('ring-2','ring-blue-300'), 1200);
        }
        qs('#searchUserId').addEventListener('input', (e) => {
            const value = e.target.value.trim();
            if (!value) { resetTable(); return; }
            // Ensure we always start from original order before applying
            resetTable();
            reorderForSearch(value);
        });
    })();
</script>
